import { PrimarySelectComponent } from './primary-select.component';

describe('PrimarySelectComponent', () => {
  let component: PrimarySelectComponent;

  beforeEach(() => {
    component = new PrimarySelectComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
